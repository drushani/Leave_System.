# 複製這個檔案為 .env，然後填入你自己的設定值。
# .env 已經被加進 .gitignore，不會被提交到版本控制裡（裡面會有密碼，不要外流）。

# ---------- 基本設定 ----------
# 正式環境（NODE_ENV=production）一定要設定，否則伺服器不會啟動
SESSION_SECRET=請改成一長串隨機亂數字串

# NODE_ENV=production
# PORT=3000
# UPLOAD_DIR=/path/to/persistent/uploads

# ---------- 寄信設定（忘記密碼、審核結果通知）----------
# 不填的話系統照樣能跑，只是不會真的寄信，通知內容只會印在伺服器的 log 裡。

# 方式一：Gmail（MAIL_PASS 要用「應用程式密碼」，不是 Gmail 登入密碼；
#         Google 帳戶要先開兩步驟驗證，才能到 https://myaccount.google.com/apppasswords 產生）
# MAIL_PROVIDER=gmail
# MAIL_USER=you@gmail.com
# MAIL_PASS=xxxxxxxxxxxxxxxx
# MAIL_FROM="請假系統 <you@gmail.com>"

# 方式二：公司內部信箱 / 自架 SMTP 伺服器（例如 Exchange、Postfix，跟公司 IT 要下面這些設定）
# MAIL_PROVIDER=smtp
# MAIL_HOST=mail.yourcompany.com
# MAIL_PORT=587
# MAIL_SECURE=false
# MAIL_USER=leave-system@yourcompany.com
# MAIL_PASS=xxxxxxxx
# MAIL_FROM="請假系統 <leave-system@yourcompany.com>"
